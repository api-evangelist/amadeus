naftiko: "1.0.0-alpha2"
info:
  label: "Tours and Activities — Retrieve"
  description: 'Tours and Activities — Retrieve. 1 operation(s). Lead operation: Amadeus Retrieve One Activity by its ID. Self-contained Naftiko capability covering one Amadeus business surface.'
  tags:
    - Amadeus
    - Travel
    - Retrieve
  created: "2026-05-29"
  modified: "2026-05-29"
binds:
  - namespace: env
    keys:
      AMADEUS_TOKEN: AMADEUS_TOKEN
capability:
  consumes:
    - type: http
      namespace: tours-and-activities-retrieve
      baseUri: https://test.api.amadeus.com/v1
      description: Tours and Activities — Retrieve business capability. Self-contained, no shared references.
      authentication:
        type: bearer
        token: '{{env.AMADEUS_TOKEN}}'
      resources:
        - name: shopping-activities
          path: '/shopping/activities/{activityId}'
          operations:
            - name: GETActivity
              method: GET
              description: Amadeus Retrieve One Activity by its ID
              outputRawFormat: json
              outputParameters:
                - name: result
                  type: object
                  value: $.
  exposes:
    - type: rest
      namespace: tours-and-activities-retrieve-rest
      port: 8080
      description: REST adapter for Tours and Activities — Retrieve. One Spectral-compliant resource per consumed operation, prefixed with /v1.
      resources:
        - path: /v1/shopping/activities/{activity-id}
          name: shopping-activities
          description: REST surface for shopping-activities.
          operations:
            - method: GET
              name: GETActivity
              description: Amadeus Retrieve One Activity by its ID
              call: tours-and-activities-retrieve.GETActivity
              outputParameters:
                - type: object
                  mapping: $.
    - type: mcp
      namespace: tours-and-activities-retrieve-mcp
      port: 9090
      transport: http
      description: MCP adapter for Tours and Activities — Retrieve. One tool per consumed operation, routed inline through this capability's consumes block.
      tools:
        - name: retrieve-one-activity-its-id
          description: Amadeus Retrieve One Activity by its ID
          hints:
            readOnly: true
            destructive: false
            idempotent: true
          call: tours-and-activities-retrieve.GETActivity
          outputParameters:
            - type: object
              mapping: $.
